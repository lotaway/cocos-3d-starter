# Cocos3d项目

# 目录结构

/assets 可用资源
    /Animations 包含各种动画动作片段，是cosos自动生成的，需要在cocos编辑器里编辑，不可直接修改
    /enums 各种枚举，例如人物状态
    /Scripts 可用的js/ts脚本，包含各种人物控制器，以Controller结尾

*.meta 所有这类结尾的文件是cosos自动生成的，不可直接修改